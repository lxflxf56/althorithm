package leetcode.page1;

import util.ListNode;


//Sort a linked list in O(n log n) time using constant space complexity.
public class SortList {
    public ListNode sortList(ListNode head) {
        return ListNode.sortList(head);
    }
}
